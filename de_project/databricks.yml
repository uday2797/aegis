bundle:
  name: aegis-de-project
  # Redeployed to reset failing_notebook to broken state for AEGIS demo

workspace:
  host: https://dbc-c486d425-ef89.cloud.databricks.com/

include:
  - resources/jobs/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      root_path: ~/.bundle/${bundle.name}/dev

  prod:
    mode: production
    workspace:
      root_path: ~/.bundle/${bundle.name}/prod
